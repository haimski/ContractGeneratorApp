# Production Environment Variables (Vercel)
# Copy this file to .env.production for production builds
# API calls use the same domain via /api routes (serverless functions)

# Leave VITE_API_URL empty for production (uses same domain)
VITE_API_URL=
